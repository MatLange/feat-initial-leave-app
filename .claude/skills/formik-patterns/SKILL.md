---
name: formik-patterns
description: REPLACED — This skill was for Formik React forms. Use /cap-handlers for CAP service handler patterns including input validation and error handling.
---

# This Skill Has Been Replaced

This project has migrated from React Native / Formik to SAP CAP.

## Use instead:

- **`/cap-handlers`** — For CAP service handler patterns including `before` validation hooks, `req.reject()` / `req.error()` for input errors, custom actions, and OData CRUD
- **`/fiori-elements`** — For Fiori Elements form configuration (Object Page edit mode, field validation annotations)
