---
name: react-ui-patterns
description: REPLACED — This skill was for React hooks and UI patterns. Use /ui5-patterns for UI5 controller/view patterns or /fiori-elements for Fiori Elements configuration.
---

# This Skill Has Been Replaced

This project has migrated from React to SAP CAP with Fiori Elements and UI5.

## Use instead:

- **`/fiori-elements`** — For Fiori Elements floorplan configuration, annotations, List Report / Object Page patterns (replaces React data fetching and list/detail patterns)
- **`/ui5-patterns`** — For custom UI5 controllers, views, fragment dialogs, OData model binding, and event handling (replaces React hooks and custom components)
- **`/cap-handlers`** — For server-side error handling and loading state logic (CAP handles this via OData protocol, not client-side state)
