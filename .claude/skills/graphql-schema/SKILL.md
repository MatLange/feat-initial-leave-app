---
name: graphql-schema
description: REPLACED — This skill was for GraphQL/Apollo. Use /cds-data-model for CDS entity design and /cap-handlers for service handler patterns (CAP uses OData, not GraphQL).
---

# This Skill Has Been Replaced

This project has migrated from GraphQL/Apollo to SAP CAP with OData.

## Use instead:

- **`/cds-data-model`** — For CDS entity definitions, associations, compositions, managed aspects, and data annotations (the CDS model IS the OData schema)
- **`/cap-handlers`** — For service handler patterns including CQL queries, custom actions/functions, and OData CRUD operations
- **`/fiori-elements`** — For configuring the UI layer that consumes the OData service
